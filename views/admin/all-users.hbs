<section>
    <div class="container">
        {{!-- <div class="d-flex justify-content-end mb-3">
            <a href="/admin/add-user" class="btn btn-primary">Add User</a>
        </div> --}}
        <table id="usersTable" class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    {{!-- <th scope="col">Action</th> --}}
                </tr>
            </thead>
            <tbody>
                {{#each users}}
                <tr>
                    <td>{{inc @index}}</td>
                    <td>{{this.Name}}</td>
                    <td>{{this.Email}}</td>
                    {{!-- <td>
                        <a class="btn btn-sm btn-primary" href="/admin/edit-user/{{this._id}}">Edit</a>
                        <a class="btn btn-sm btn-danger" href="/admin/delete-user/{{this._id}}" onclick="return confirm('Are you sure?')">Delete</a>
                    </td> --}}
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</section>
